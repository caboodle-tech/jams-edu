--- Fenced `jams-*` div rendering is handled in `src/jamsedu.js` (extract before fragment Pandoc, merge after).
--- Keep this file as a no-op filter so existing `--lua-filter` wiring remains valid.
return {}
