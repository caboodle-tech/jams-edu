{
    "devDependencies": {
        "@eslint/json": "^1.2.0",
        "@html-eslint/eslint-plugin": "^0.59.0",
        "@html-eslint/parser": "^0.59.0",
        "eslint": "^10.2.1"
    },
    "scripts": {
        "build": "jamsedu --build",
        "build:debug": "jamsedu --build --verbose",
        "watch": "jamsedu --watch",
        "watch:debug": "jamsedu --watch --verbose"
    },
    "type": "module"
}
